# PTP Count — AI自動検出への進め方

## 目的
写真内の「残っているPTPポケット」を1錠=1個として検出し、AIが仮カウントした結果を人が追加・削除して確定できるようにする。

## 方針
最終的には、PTP専用の軽量物体検出モデルを学習し、iPhoneのブラウザ上で推論する構成を目指す。患者画像を外部AIサービスへ送らない構成を優先する。

## Phase 1: 教師データ収集
現行アプリ v2.0 の「教師データを書き出す」を使う。

1. PTP写真を読み込む。
2. すべての残薬ポケットを手動で正しくタップする。
3. 計数を確定する。
4. 「教師データを書き出す」を押す。
5. `.ptp.json` ファイルをiPhoneの「ファイル」などに保存する。

1ファイルには、縮小済みJPEG画像、画像サイズ、各PTPポケット中心座標、正解個数が入る。

### 最初の目安
- 動作確認用: 30〜50画像
- 実用性を上げる段階: 100〜300画像程度を目標

同じ薬だけに偏らず、次の違いを含める。
- 透明/銀色/色付きPTP
- 丸型/楕円/カプセル型など
- 1錠ずつ切り離したものと複数錠シート
- 少数〜多数
- 光の反射や背景の違い

空包は撮影前に除外する現在の運用を前提とする。

## Phase 2: 学習データ化
収集した `.ptp.json` をまとめ、物体検出用データセットへ変換する。現行の中心点ラベルから、学習用のバウンディングボックスを生成または追加調整する。

## Phase 3: モデル学習
軽量YOLO系モデルを候補とする。Google Colab等で学習し、検証用画像で精度を評価する。

評価候補:
- 完全一致率
- 平均絶対誤差
- 過小計数率 / 過大計数率
- 1枚あたりの処理時間
- PTP形状・色・切離し状態ごとの誤差

## Phase 4: iPhoneへ組み込み
学習済みモデルをWebで実行できる形式に変換し、GitHub Pages上のPTP Countへ組み込む。

想定UI:
1. 写真を撮影
2. 「AI自動検出」
3. AI候補に番号を自動表示
4. 人が追加/削除
5. 確定・保存

AIの結果をそのまま採用せず、人が最終確認する設計を維持する。

## 情報管理
教師データとして保存する写真には、患者氏名、患者ID、処方箋、薬袋ラベルなど個人を識別できる情報を写し込まない。院内で実データを研究利用する場合は、所属施設の研究・情報管理ルールに従う。
